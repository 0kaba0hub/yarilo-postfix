{{- if .Values.postsrsd.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: relay-postsrsd
  namespace: {{ .Release.Namespace }}
  labels:
    app: relay-postsrsd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: relay-postsrsd
  template:
    metadata:
      labels:
        app: relay-postsrsd
    spec:
      containers:
        - name: postsrsd
          image: "{{ .Values.image.postsrsd.repository }}:{{ .Values.image.postsrsd.tag }}"
          imagePullPolicy: {{ .Values.image.postsrsd.pullPolicy }}
          ports:
            - containerPort: 10001
              name: milter
            - containerPort: 10003
              name: socketmap
          env:
            - name: SRS_DOMAIN
              value: {{ .Values.postsrsd.domain | default .Values.mail.domain | quote }}
            {{- if .Values.postsrsd.secretName }}
            - name: SRS_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.postsrsd.secretName }}
                  key: SRS_SECRET
            {{- end }}
            {{- if .Values.postsrsd.redisAddr }}
            - name: SRS_REDIS_ADDR
              value: {{ .Values.postsrsd.redisAddr | quote }}
            {{- end }}
            {{- if .Values.postsrsd.debug }}
            - name: SRS_DEBUG
              value: "on"
            {{- end }}
          resources:
            {{- toYaml .Values.resources.postsrsd | nindent 12 }}
{{- end }}
