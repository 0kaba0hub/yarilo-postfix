{{- if .Values.mx.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: relay-rspamd-mx
  namespace: {{ .Release.Namespace }}
  labels:
    app: relay-rspamd-mx
spec:
  type: ClusterIP
  selector:
    app: relay-rspamd-mx
  ports:
    - name: milter
      port: 11332
      targetPort: milter
    - name: web
      port: 11334
      targetPort: web
{{- end }}
